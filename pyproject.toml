[project]
name = "npa-bot"
version = "0.1.0"
description = "NPA Scorer - a Discord bot that manages competitive rankings for ping pong leagues."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "discord-py>=2.0.0",
    "aiohttp>=3.8.0",
    "python-dotenv>=1.0.0",
]
